server:
  port: 8083

spring:
  application:
    name: auth-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://keycloak:8080/realms/parcel-realm}  # Change to keycloak:8080

keycloak:
  auth-server-url: ${KEYCLOAK_URL:http://keycloak:8080}  # This is correct
  realm: ${KEYCLOAK_REALM:parcel-realm}

  # For resource server
  resource: ${KEYCLOAK_CLIENT:auth-service}
  credentials:
    secret: ${KEYCLOAK_SECRET:your-client-secret}

  # For admin client
  admin:
    client-id: ${KEYCLOAK_ADMIN_CLIENT:admin-cli}
    username: ${KEYCLOAK_ADMIN_USER:admin}
    password: ${KEYCLOAK_ADMIN_PASSWORD:admin123}
    client-secret: ${KEYCLOAK_ADMIN_SECRET:}

springdoc:
  api-docs:
    path: /api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    display-request-duration: true
    tags-sorter: alpha
    operations-sorter: alpha
    url: /api-docs
    config-url: /api-docs/swagger-config
    urls-primary-name: Auth Service API
    disable-swagger-default-url: false

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/

logging:
  level:
    com.sparrow.authservice: DEBUG
    org.keycloak: DEBUG  # Change to DEBUG for troubleshooting
    org.springdoc: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
      base-path: /management
  endpoint:
    health:
      show-details: always
    metrics:
      enabled: true
    info:
      enabled: true